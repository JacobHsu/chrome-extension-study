// browser_action 檢查彈跳式視窗
document.addEventListener('DOMContentLoaded', function(dcle) {

    // var port = chrome.runtime.connect({name: "longlived"});

    // port.onMessage.addListener(function(msg) {
    //     console.log('popup.js connect', msg)
    //     document.querySelector('#string').innerText = "msg: " + msg.question
    //     document.querySelector('#in').innerText = "input msg: " + msg.in
    // });

    // Simple one-time requests  On the receiving end
    // chrome.runtime.onMessage.addListener(function(message, sender, sendResponse) {
    //     console.log('[in popup.js] message', message); 
    //     document.querySelector('#popup').innerText = message.content
    //     //console.log(sender); //chrome console can see
    //     //sendResponse({ content: "來自內容腳本的回覆" });
    // });

});


